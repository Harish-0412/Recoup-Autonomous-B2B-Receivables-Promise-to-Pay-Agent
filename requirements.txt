# Core application foundation
fastapi>=0.111
pydantic>=2.7
pydantic-settings>=2.3
uvicorn[standard]>=0.30

# Testing
pytest>=8.2

# --- Agent loop ---
# instructor:     validated, retry-on-failure structured output from the LLM.
# transitions:    escalation-ladder FSM; guards are enforced by the library,
#                 not by every call site remembering to check them.
# business-rules: policy rules defined as data rather than nested ifs.
instructor>=1.16
transitions>=0.9
business-rules>=1.1

# --- Integrations ---
# groq must stay >=1.0: 0.6.0 passes `proxies=` to httpx, which httpx>=0.28
# removed, so constructing a client raises TypeError.
groq>=1.7
# razorpay must stay >=2.0: 1.4.x imports pkg_resources, which modern
# setuptools no longer provides, so `import razorpay` raises ModuleNotFoundError.
razorpay>=2.0
resend>=2.4

# --- ML additions ---
scikit-learn>=1.4
xgboost>=2.0
shap>=0.45
pandas>=2.2
numpy>=1.26
joblib>=1.3
dateparser>=1.2
